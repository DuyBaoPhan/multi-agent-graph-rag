# Redis Configuration for Highlands Coffee Robot

# Memory
maxmemory 256mb
maxmemory-policy allkeys-lru

# Persistence (append-only file)
appendonly yes
appendfsync everysec

# Network
bind 0.0.0.0
protected-mode no
timeout 300

# Logging
loglevel notice
